/// Совместимость: AppMode и ModeController живут в core/mode_controller.dart.
library;

export 'core/mode_controller.dart';

import 'core/mode_controller.dart';

/// Старое имя для ModeController.
typedef VoiceModeSelector = ModeController;
